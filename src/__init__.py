"""Project code package root.

We keep a top-level `src` package so we can run modules via:
  python -m src.synthpop ...

This matches the workspace convention used on wsA (see docs/WORKSTATION_GUIDE.md).
"""

