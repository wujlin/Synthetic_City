numpy
pandas
# PyTorch wheels depend on CUDA / Python version. If pip install fails, install via conda or follow
# https://pytorch.org/get-started/locally/ for the correct command.
torch

# Geo stack (only needed for building/parcels scripts).
geopandas
pyproj
shapely
