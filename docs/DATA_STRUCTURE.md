# 数据目录结构（Data Structure）

> 口径：城市维度隔离 + `raw/interim/processed/outputs` 四层结构。

```text
data/
  <city>/
    raw/
    interim/
    processed/
    outputs/
```

## Detroit 示例

Detroit 的推荐目录树、关键表 schema 与 run 产物组织方式见：`docs/detroit_code_data_structure.md`。

