# AGENTS.md — 研究与写作工作流规范

---

## 一、核心理念与原则

> **简洁至上**：恪守 KISS 原则，崇尚简洁与可维护性，避免过度工程化与不必要的防御性设计。

> **深度分析**：立足于第一性原理剖析问题，并善用工具以提升效率。

> **事实为本**：以事实为最高准则。若有任何谬误，恳请坦率斧正，助我精进。分析问题实事求是，务实胜于教条。

**开发工作流**

> **渐进式开发**：通过多轮对话迭代，明确并实现需求。在着手任何设计或编码工作前，必须完成前期调研并厘清所有疑点。

> **结构化流程**：严格遵循"构思方案 → 提请审核 → 分解为具体任务"的作业顺序。

**输出规范**

> **语言要求**：所有回复、思考过程及任务清单，均须使用中文。

> **固定指令**：`Implementation Plan, Task List and Thought in Chinese`

**环境备注**：WSL 中已安装 miniconda 管理环境，可自行安装各种包；本地有一张 4060 独立显卡。

**风险提醒**

> **删除前务必备份**：在移动/删除/归档任何文件或目录前，先确认已有备份或纳入版本控制；若不确定是否可恢复，需暂停操作并提示用户确认备份方案，避免不可逆的数据丢失。

---

## 二、高质量输出的思维原则

### 2.1 核心转换：从"做了什么"到"发现/解决了什么"

> **警惕执行者思维**：执行者的逻辑是"我做了 A、B、C"（过程驱动），高质量输出的逻辑是"问题是 X，答案是 Y，这意味着 Z"（问题驱动）。无论是写代码、做分析还是写文档，都要问：**这是在回答一个问题，还是在描述一个步骤？**

| 执行者思维 ❌ | 问题驱动思维 ✓ |
|---|---|
| "我实现了函数 X" | "这个函数解决了 Y 问题" |
| "我验证了模型" | "我发现了 X，这揭示了 Y" |
| 按时间/方法顺序组织 | 按问题递进组织 |
| 列举所有工作（First, Second, Third） | 整合核心洞见 |
| 显式预告下一步 | 逻辑自然过渡 |

### 2.2 叙事主线与逻辑衔接

> **一条主线贯穿始终**：任何输出都应该有一个清晰的故事线：问题是什么 → 为什么难/重要 → 核心洞见 → 证据/实现 → 意义/影响。每一部分都服务于这条主线。

**逻辑衔接的层次**：
1. **句子间**：上一句的结论自然引出下一句
2. **段落/模块间**：每个模块回答一个递进的问题
3. **全局**：开头提出的问题在中间被回答，在结尾被升华

**自检方法**：
- 删除所有显式过渡后，逻辑是否仍然连贯？
- 每个模块的开头是否点明它要解决的问题？
- 每个模块的结尾是否是 take-home message（而非下一模块的预告）？

### 2.3 重点突出与信息层次

> **核心洞见要最显眼**：读者/用户的注意力是稀缺资源。核心内容应该在最显眼的位置，而不是被埋在细节中。

**信息分层原则**：
- **核心层**：只放理解主要论点所必需的信息
- **支撑层**：实现细节、方法说明
- **参考层**：完整推导、边界情况、robustness checks
- **元数据层**：配置、参数、版本信息

**措辞层次**：
- 核心发现/结论：assertive 语气（"是"、"reveals"、"shows"）
- 支撑证据：neutral 语气（"able"、"consistent with"）
- 局限/边界：honest 语气（"constrained by"、"limited to"）

### 2.4 常见思维陷阱

1. **Defensive 思维**："为了解决这个问题"、"我们尝试"——暗示你在被动响应，而非主动发现
2. **验证序列**：做 X → 验证 X → 再验证 X——让人觉得你在证明自己对，而非推进理解
3. **清单式输出**：First, Second, Third——像汇报工作，不像提供洞见
4. **细节污染**：在核心层放实现细节——模糊重点，增加认知负担
5. **重复**：结尾重述开头——结尾应该是升华，不是总结

---

## 三、研究方法论

### 3.1 识别真正的贡献

> **"我们用了 X"不是贡献，"我们发现了 Y"才是。** 如果你的贡献听起来像是"我们搭了一个两阶段 pipeline"，那只是工程描述。真正的贡献藏在更深处——是什么问题性质（problem structure）使得这种分解成为唯一正确的选择？

**三层递进**：
1. **问题诊断**（最有价值）：发现问题中存在什么结构性质，使得某种分解是必要的
2. **设计原则**：从问题结构中推导出的、可迁移到其他场景的一般性规律
3. **方法实例**：具体方法只是验证上述原则的载体，不是贡献本身

**自检**：如果把你的方法名换成另一个名字，贡献陈述是否还成立？如果只在名字层面成立，说明你还停留在"方法描述"而非"洞见表达"。

### 3.2 理论锚定：每个声明都需要根基

> **Intro 里不允许存在无锚的声明。** 每一个核心论断必须有以下三种锚之一：

| 锚类型 | 举例 |
|---|---|
| 文献引用 | 引用已有理论或实证工作，说明某个约束/规律的存在 |
| 实验证据 | 用本文的实验数据直接支撑论断（附具体数字） |
| 逻辑推演 | 从问题定义出发，通过逻辑推理得出不可避免的结论 |

**反面典型**：
- "Both families represent genuine progress"——空洞评价，无信息量，删掉
- "This approach is qualitatively different"——"qualitatively"是逃避具体说明的遮羞布，换成具体维度上的差异
- 工程调参味的措辞（如"capacity control""hyperparameter tuning"）——应重构为科学原理层面的表述

### 3.3 文献评估的三级制

拿到一批文献后，不要全部引用，按对核心论点的支撑力度分级：

| 级别 | 判断标准 | 处理方式 |
|---|---|---|
| **Must-cite** | 直接支撑核心论点的理论基础或实证对照 | 正文中引用，明确说明其与本文的关系 |
| **May-cite** | 提供有用的理论背景或佐证，但非核心 | Related Work 中提及，或用脚注 |
| **Skip** | 主题相关但不增强论证，或领域偏远 | 不引用，避免 citation padding |

**原则**：引用不是展示你读了多少论文，而是构建你论证的砖块。每一条引用都应该服务于主线。

### 3.4 区分"工程改进"与"科学发现"

| 工程改进 | 科学发现 |
|---|---|
| "我们加了 trick X，性能提升 Y%" | "失败模式从 A 变为 B，表明问题性质发生了根本转变" |
| "超参 d=64 效果最好" | "d 与目标空间内在维度存在匹配关系，过低/过高时呈非对称衰减" |
| "K=16 接近饱和" | "K 的边际收益递减说明目标空间的有效维度是有限的" |

工程改进可以写在 ablation 里，但不应作为核心贡献。核心贡献必须是对问题本身的理解。

---

## 四、学术写作原则

### 4.1 核心思维：从技术报告到科学叙事

> **技术报告**的逻辑是"我们做了什么"（方法驱动），**科学论文**的逻辑是"我们发现了什么，这意味着什么"（问题驱动）。每写一段都要问：这是在回答一个科学问题，还是在描述一个技术步骤？

| 技术报告思维 ❌ | 科学叙事思维 ✓ |
|---|---|
| "We validate/confirm/test..." | "We find/show/identify..." |
| Results 按方法顺序排列 | Results 按科学问题递进排列 |
| 每节末尾显式预告下一节 | 逻辑自然过渡 |
| 列举贡献（First, Second, Third） | 用 narrative statement 整合核心发现 |

### 4.2 叙事主线（全文级）

> 从 Title 到 Conclusion，读者应该能感受到一个连贯的故事：提出问题 → 为什么难 → 我们的洞见 → 证据 → 意义。每一节服务于这条主线，而不是独立的技术展示。

**三个层次**：
1. **段落间**：上一段的结论自然引出下一段的问题（不需要 "We next..."）
2. **Section 间**：每节回答一个递进的问题，读者读完自然想知道下一个
3. **全文**：Introduction 提出的问题在 Results 中被回答，在 Conclusion 中被升华（不是重复）

### 4.3 英文写作实操规范

以下是在实际写作中反复踩过的坑，形成的硬规则：

#### 标点与符号

| 规则 | 错误示例 | 正确示例 |
|---|---|---|
| **破折号（em-dash）不可滥用**：一段最多一处；优先用分号、括号或重构句子 | "methods — which rely on — local decisions — fail" | "methods that rely on local decisions fail" |
| **e.g. / i.e. 后必须加逗号**；句首不使用，改用 "for example" / "that is" | "e.g. RNN and Transformer" / 句首 "E.g., ..." | "e.g., RNN and Transformer" / "For example, RNN and..." |
| **缩写首次出现必须展开** | "The VAE bottleneck..." (首次) | "The variational autoencoder (VAE) bottleneck..." |

#### 句子与段落结构

| 规则 | 说明 |
|---|---|
| **每段首句是该段的 claim** | 不是背景铺垫，而是直接亮明该段要论证的观点 |
| **每段末句是 take-home message** | 不是下一段的预告（"We next examine..."），而是本段结论 |
| **删掉空洞桥接句** | "Both families represent genuine progress"——如果删掉后逻辑不断，就说明它不承载信息 |
| **模糊限定词必须具体化** | "qualitatively different" → "differ fundamentally in dimensionality and function" |
| **避免工程化措辞** | 把"调参"层面的表述重构为科学原理层面的表述 |

#### Introduction 专项规则

| 规则 | 说明 |
|---|---|
| **只放概念层面论述** | 不提具体组件名（Perceiver、cross-attention 等属于 Method） |
| **贡献陈述用叙事整合** | 不用 First/Second/Third 清单，用一段话讲清核心洞见 |
| **不说 "We propose"** | 用 "identifies" / "reveals" / "shows"——让发现自己说话 |
| **Intro 末尾不是方法概述** | 末尾应是核心发现的 assertive statement，不是"本文结构如下" |

#### 措辞层次

- 核心发现：assertive（"identifies", "reveals", "shows"）
- Robustness check：neutral（"confirms", "persists", "remains"）
- Limitations：honest（"constrained by", "limited to"）

### 4.4 常见写作陷阱速查

1. **Defensive writing**："To address this gap" / "We validate"——暗示在填补空白，而非做发现
2. **验证序列**：Theory → Validate → Validate → Validate——读者觉得你在证明自己对
3. **贡献清单**：First, Second, Third——像 PhD 答辩，不像期刊文章
4. **细节污染**：正文中放代码路径、API 参数、具体 grid 数值——这些属于 Appendix
5. **重复**：Conclusion 重述 Introduction——Conclusion 应该是升华和 implications，不是 summary
6. **被动虚化**："It can be observed that..."——直接说 "X shows Y"
7. **Hedging 过度**："It seems that perhaps the model might..."——如果有证据就 assertive 地说

---

## 五、实验管理原则

### 5.1 实验设计

| 原则 | 说明 |
|---|---|
| **多种子是底线** | 任何对数字的主张都需要 ≥3 seeds 的 mean±std |
| **Sweep 必须覆盖最优点两侧** | 如 d=64 是甜蜜点，必须同时有 d=32 和 d=128 确认非单调性 |
| **控制变量** | 每组实验只变一个因素，其余对齐 |
| **先跑 pilot 再跑全量** | 新配置先用小数据/少 epoch 验证可行性，再投入完整训练 |

### 5.2 结果分析

> **先理解"它揭示了什么"，再决定"怎么呈现"。** 不要拿到数字就直接往表里填——先问：这个结果是否符合预期？不符合的话，是实验 bug 还是真正的发现？

**必做的检查**：
- **质疑异常值**：0.0 或 NaN 的指标可能是计算缺失而非真实结果（某个 pipeline 阶段未跑完）
- **交叉验证一致性**：多种子结果的方差是否合理？是否有 outlier seed？
- **检查上游依赖**：上游产物（如 pretrained checkpoint）是否已收敛？配置差异是否导致种子间不可比？

### 5.3 结果呈现

| 原则 | 说明 |
|---|---|
| **数字服务于论点** | 不是展示所有跑过的实验，而是选择最能支撑核心声明的证据 |
| **非对称性比绝对值更重要** | "d 偏小温和衰减，d 偏大灾难性衰减"比"d=X 最优"更有洞见 |
| **失败模式比成功指标更有故事** | failure mode decomposition 揭示的是方法的结构性局限，而非简单的"准确率不够高" |
| **边际收益递减比最优值更有故事** | 超参 sweep 的饱和行为揭示目标空间的有效维度 |

---

## 六、协作工作流

### 6.1 角色分工

本项目涉及三方协作：

| 角色 | 职责 |
|---|---|
| **用户（我）** | 把控写作质量、概念清晰度、论文叙事主线 |
| **AI 助手** | 概念分析、文献评估、文本起草与修改、实验结果解读、代码编写 |
| **Partner** | 在工作站执行实验、文献检索、数据处理 |

### 6.2 文献检索协作

向 partner 发出的文献检索请求应该是**结构化的 prompt**，明确指定：
1. 要搜索的**核心概念**（用术语而非自然语言描述）
2. **目标**：这些文献要用来支撑什么论点？
3. **排除范围**：已有哪些文献？哪些方向不需要？

拿回结果后，用三级制（Must-cite / May-cite / Skip）快速评估，不要全部引用。

### 6.3 实验请求协作

向 partner 发出的实验请求应包含：
1. **完整的命令行或脚本**（不要只说"跑一下 d=128"）
2. **预期产物**：跑完后需要哪些 JSON/checkpoint/log？
3. **优先级标注**：P0（论文主表必需）、P1（ablation 需要）、P2（appendix 可选）
4. **预估时间**：基于已有 benchmark 估算 GPU 时间

---

## 七、自检清单

### 写作自检

- [ ] Introduction 中没有 First/Second/Third 贡献清单
- [ ] 没有无锚的声明（每个论断有文献、实验证据或逻辑推演支撑）
- [ ] 没有空洞桥接句（删掉后逻辑仍连贯的句子都应删掉）
- [ ] 缩写首次出现已展开
- [ ] e.g./i.e. 后面有逗号；句首用 "For example" / "That is"
- [ ] 破折号每段不超过一处
- [ ] 每段首句是 claim，末句是 take-home message
- [ ] Conclusion 不重复 Introduction（是升华，不是 summary）
- [ ] 所有数字与实验产物 JSON 一致

### 实验自检

- [ ] 核心结果有 ≥3 seeds 的 mean±std
- [ ] Sweep 覆盖了最优点两侧
- [ ] 异常值（如 0.0）已被排查，确认是真实结果而非数据缺失
- [ ] 上游依赖（checkpoint 收敛性、配置一致性）已核实

### 研究自检

- [ ] 贡献陈述是"发现了什么"而非"做了什么"
- [ ] 核心洞见换一个方法名后仍然成立
- [ ] 能用一句话说清"为什么这个分解是唯一正确的"（而非"我们选择了这个分解"）
- [ ] 文献引用服务于论证主线，而非 citation padding
