\documentclass[11pt]{article}

\usepackage[a4paper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{setspace}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{siunitx}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage[section]{placeins}  % prevent floats from drifting past section boundaries
\usepackage{float}              % provides [H] placement
\usepackage{flafter}            % do not place floats before their definitions

\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  citecolor=blue!50!black,
  urlcolor=blue!50!black
}

\title{Distribution-Level Diffusion Recovers Spatially\\Heterogeneous Dependence from Marginal Constraints}


\date{}

\begin{document}
\maketitle
\onehalfspacing

\begin{abstract}
Reconstructing how demographic attributes co-occur within a region---not just their individual frequencies---is a central challenge in population synthesis. The standard method, iterative proportional fitting (IPF), fills this gap by reusing a fixed national-average template, implicitly assuming that the dependence structure is the same everywhere. We show empirically that this assumption is violated: regional dependence patterns vary substantially across more than 2,400 regions in the United States. To learn these region-specific patterns, we train a conditional diffusion model to directly predict the quantity that matters---the regional joint probability vector---treating each region's observed distribution as a single training example. This approach consistently outperforms IPF across four experimental configurations (up to 59\% error reduction), with statistical significance confirmed in a five-fold spatial cross-validation. Systematic ablations reveal that performance is driven by the quality of input constraints---two-variable cross-tabulations provide up to 44\% additional improvement over one-variable summaries---establishing constraint informativeness, not model capacity, as the critical resource for dependence recovery.
\end{abstract}

%% ====================================================================
\section{Introduction}
\label{sec:intro}

The creation of a synthetic population by reconstructing the joint distribution of demographic attributes within regions from aggregated data (i.e., data reporting regional marginal distributions of individual attributes) has long been regarded as a foundation of urban simulation, transportation modeling, and policy analysis \cite{mueller2011,barthelemy2013,la2025,chapuis2022}. However, aggregated data drastically under-specify the full picture from a statistical perspective. For example, when creating a synthetic population with five attributes and four values per attribute, the joint distribution contains more than one thousand potential combinations, whereas current methods typically rely on one-dimensional marginals alone. As a result, the underlying dependence structure---that is, the copula \cite{sklar1959} describing how multiple attributes co-occur---may be missed. The resulting synthetic population can therefore contain internal bias, such as assigning an impossible combination of attributes to a single synthetic individual.

Recovering a joint distribution from marginals alone is inherently difficult because the copula is hard to reconstruct \cite{frogner2019}. Existing population-synthesis methods nevertheless attempt to address this problem through two broad strategies: synthetic reconstruction and combinatorial optimization \cite{chapuis2022}. In the synthetic-reconstruction family, methods such as Hierarchical Sampling (HS) \cite{jiang_large-scale_2024}, Iterative Proportional Fitting (IPF) \cite{predhumeau_synthetic_2023}, and Iterative Proportional Updating (IPU) \cite{ipu2009} recover joint distributions from aggregated data (e.g., census-tract-level data) by matching target marginals while preserving the seed copula structure \cite{choupani2016,lovelace2015}. In the combinatorial-optimization family \cite{harland2012}, one extends real individual-level microdata or adjusts existing synthetic individuals to match target joint distributions through fitness evaluation and optimized sampling procedures.

However, these methods share a common limitation: the copula structure is extracted from aggregated data or real individual-level microdata and then applied uniformly across regions. This works when regions are demographically similar, but when regions are heterogeneous, a fixed copula can misrepresent local dependence and introduce internal bias into the resulting synthetic population \cite{roxburgh2025}. A related issue is spatial non-stationarity \cite{krupskii2018,mondal2024,bastin2023}, which fails to reconstruct the distribution of spatial heterogeneity related to the different combinations of demographic attributes. For example, a university region with a distinctive ``young + low-income + high-education'' profile does not share the same age--income--education dependence pattern as a manufacturing region. To demonstrate the spatial heterogeneity, we conduct an analysis by extracting the dependence structures (i.e., copula structures) across the 2,456 Public Use Microdata Areas (i.e., PUMAs) in the United States and comparing each regional dependence structure with the national average of total variation distance (i.e., TVD) of 0.19, which is far from negligible and far from uniform across regions (Fig.~\ref{fig:heterogeneity}).

\begin{figure}[!bhtp]
\centering
\includegraphics[width=\textwidth]{figures/fig_01_map.pdf}
\caption{Spatial heterogeneity of dependence structure across regions in the United States.
Choropleth of PUMA-level TVD to the national-average joint distribution ($K=32$, five demographic attributes).}
\label{fig:heterogeneity}
\end{figure}

To handle such copula structures, optimization methods such as integer least squares programming have been used to create synthetic populations for the United States from Census Block Group data \cite{lin2023}. Parametric approaches instead use statistical inference to learn the joint relationship among hidden attributes in the data. Examples include methods based on Markov Chain Monte Carlo \cite{farooq2013} and Bayesian-network sampling \cite{zhou_creating_2022}. A persistent limitation, however, is that the resulting spatial non-stationarity remains unresolved because the same set of parameters or the same structural template is used throughout the synthesis process.

Recently, generative models have provided a new possibility for recovering the correct copula structure while addressing spatial non-stationarity in synthetic populations \cite{ang2025,sane2025}. Among them, diffusion models are particularly well suited to this task because they can learn the joint distribution through an iterative denoising process, without requiring a predefined copula template or hand-specified parameter settings \cite{ho2020}. Conditional diffusion is especially relevant because it can use regional demographic summaries as constraints while allowing the dependence structure itself to vary across space. In this paper, we therefore ask whether a diffusion model trained on PUMA-level distributions can recover region-specific copulas more accurately than methods that rely on a single national seed.



%The field has developed effective solutions to this problem, each under specific conditions. IPF-based reweighting \cite{deming1940,bishop1975,choupani2016,lovelace2015} recovers accurate joints when a representative seed table is available: IPF adjusts the seed to match target marginals while preserving the seed's dependence structure \cite{csiszar1975}. Combinatorial optimization \cite{harland2012} can match target distributions when the candidate microdata pool covers the target support. Bayesian network sampling \cite{farooq2013} and integrated frameworks such as simPop \cite{templ2017} succeed when a correct parametric or structural prior can be specified.

%In practice, however, per-record training losses optimize marginal accuracy far more efficiently than dependence accuracy. The reason is structural: if 80\% of a region's residents are young, then 80\% of training records contain ``young''---the marginal frequency is directly encoded in the data frequency, producing a strong, consistent gradient. Dependence, by contrast, concerns the relative frequency of attribute combinations: a single training record (young, low-income) is equally consistent with $P(\text{low-income}\mid\text{young})=0.8$ and $P(\text{low-income}\mid\text{young})=0.7$---both copulas frequently produce this record, so per-record losses cannot distinguish between them. The gradient signal for dependence is therefore weak and noisy. We confirmed this empirically: a person-level conditional diffusion model, even with exact region identity as input, produced copula TVD of 0.159 across Michigan's 68 regions---systematically worse than the 0.137 obtained by na\"ively reusing the training-region average (Supplementary Fig.~S1).

%---a form of spatial non-stationarity well recognized in the copula literature \cite{krupskii2018,mondal2024,bastin2023}. Moving beyond the direct reweighting or resampling of existing records, statistical inference is another approach to dealing with such issue 
%The direct route eliminates this signal asymmetry.We train a conditional denoising diffusion model to predict the regional joint probability vector---a single vector whose entries encode both marginal frequencies and their couplings. 

%In this formulation, copula errors are immediately visible in the loss function and optimized with the same directness as marginal errors. Each of 2,456 observed regional distributions becomes one training example, and the model learns to map marginal (and optionally pairwise) constraints to the full joint \cite{floto2023,avdeyev2023}. In practice, the diffusion model produces a region-specific joint distribution from marginal inputs; one round of IPF then enforces exact marginal consistency, and standard sampling yields individual synthetic records. The IPF baseline follows the same post-processing but starts from a national-average joint distribution, so the comparison isolates how well each approach captures region-specific dependence (Fig.~\ref{fig:schematic}).Distribution-level diffusion consistently outperforms global-seed IPF across all five tested configurations with up to 256 joint cells, achieving up to 59\% error reduction. A five-fold spatial cross-validation on held-out regions confirms statistical significance ($p = 0.031$). Systematic ablations reveal that the critical determinant of performance is not model architecture but the informativeness of input constraints: two-variable cross-tabulations provide up to 44\% additional improvement over one-variable summaries alone.

%% ====================================================================

% start to revise the result
\FloatBarrier
\section{Methods}

\subsection{Overview}
%The goal of this work is to learn region-specific dependence structures from data, replacing the fixed global template used by standard methods. 
As discussed in Section~\ref{sec:intro}, the goal of this work is to propose a framework to reconstruct a synthetic population data where its five attributes follow the joint distribution of multiple demographic variables using PUMS data. In addition, this framework could generate the combinations of synthetic population attributes with realistic spatial heterogeneity. The framework comprises three stages as shown in Figure~\ref{fig:framework}. 

\textbf{Stage~1: Distribution-level representation.}
As noted, five attributes will be generated for the resulting synthetic population, thus, in this stage we construct the empirical joint probability vector for the five attributes (e.g., age, sex, income, education, employment status) to represent copula. Specifically, for each of 2,456 PUMAs regions in the United States, the vector is constructed based on $\mathbf{p}\in\Delta^{K-1}$ over $D$ discretized demographic attributes, where $K=\prod_{d=1}^{D}B_d$ is the number of joint cells. We treat each region's $\mathbf{p}$ as a single training example, directly aligning the learning objective with the evaluation metric (\S\ref{sec:training_target}).

\textbf{Stage~2: Conditional diffusion learning.}
A denoising diffusion model learns to map low-dimensional demographic constraints (specifically, one-dimensional marginals and two-dimensional cross-tabulations) to the full joint distribution using the vector generated in stage 1.
The model is trained under the standard denoising diffusion probabilistic model (DDPM) $\epsilon$-prediction objective, with constraints injected via concatenation at the denoiser input. Four conditioning modes (none, marginal, pairwise, marg+pair) form a controlled ablation ladder that isolates the information contribution of each constraint type (\S\ref{sec:condition_design}).

\textbf{Stage~3: Post-processing and evaluation.}
At inference, an ensemble of independent denoising draws is averaged and passed through softmax to produce a raw probability estimate; 




\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/fig_02_framework.jpg}
\caption{Method overview: distribution-level diffusion for population synthesis.
\textbf{(Left)~Training:} Each region is represented by an empirical joint distribution $\mathbf{p}$, transformed into log-probability space and z-score normalized to form the diffusion target $\mathbf{z}_0$. The denoiser receives the noised target $\mathbf{z}_t$, a timestep embedding, and a condition vector $\mathbf{c}$ built from marginal and optional pairwise constraints.
\textbf{(Right)~Inference:} Reverse denoising maps Gaussian noise to a region-specific raw joint estimate, which is averaged across repeated draws and then adjusted by one round of post-hoc IPF to produce the final marginal-consistent joint distribution.}
\label{fig:framework}
\end{figure}



\subsection{Data and study area}

The primary data source is the 2023 five-year American Community Survey (ACS) Public Use Microdata Sample (PUMS), covering 2,456 PUMAs across the United States. For each PUMA, we extract an empirical joint distribution over $D$ individual-level demographic attributes---age (AGEP), sex (SEX), personal income (PINCP), educational attainment (SCHL), and employment status (ESR). By discretizing these variables at varying resolutions and selecting subsets of 3 or 5 attributes, we generate state spaces ranging from $K=32$ to $K=512$, allowing us to probe the model's scaling behavior across different joint dimensionalities.

Prior to constructing the joint probability tables, continuous variables (specifically AGEP and PINCP) are discretized into categorical bins to form a finite diffusion target (Table~\ref{tab:var_attr}). The regional joint distributions are then computed by aggregating these microdata records and applying the respective survey weights.

%Table~\ref{tab:var_attr} clarifies the modeling status of each variable. In particular, AGEP and PINCP are not modeled as continuous variables in the diffusion target; they are discretized into bins before joint-table construction.

\begin{table}[!bhtp]
\centering
\small
\caption{Variable attributes and modeling representation in this study.}
\label{tab:var_attr}
\begin{tabular}{p{0.12\linewidth}p{0.17\linewidth}p{0.24\linewidth}p{0.18\linewidth}p{0.21\linewidth}}
\toprule
Variable & Raw type & Model representation & Source & Purpose \\
\midrule
AGEP & Numeric (person age) & Discrete bins only (2 or 4 bins by setting) & ACS PUMS 2023 five-year person file & Demographic axis in 3/5-var joints; contributes to dependence structure. \\
SEX & Categorical code (1/2) & Two categories (Male/Female) & ACS PUMS 2023 five-year person file & Core demographic attribute in 3/5-var joints. \\
PINCP & Numeric (personal income) & Discrete bins only (2, 4, or 16 bins by setting) & ACS PUMS 2023 five-year person file & Economic axis in 3/5-var joints; key to age--income and education--income dependence. \\
SCHL & Ordinal categorical code & Merged bins (2 or 4 bins by setting) & ACS PUMS 2023 five-year person file & Education axis in 3/5-var joints; used for higher-order dependence tests. \\
ESR & Categorical code & Merged bins (2 or 4 bins by setting) & ACS PUMS 2023 five-year person file & Employment axis in 5-var settings; increases dimensional stress. \\
\bottomrule
\end{tabular}
\vspace{2pt}
\begin{minipage}{0.95\linewidth}
\footnotesize
\textit{Notes:} ``Discrete bins only'' means the diffusion model is trained on regional joint probability vectors over binned categories, not on continuous AGEP/PINCP values.
\end{minipage}
\end{table}

A critical challenge in high-dimensional configurations (e.g., $K=512$) is cell sparsity, which can produce zero-count cells and result in numerically undefined diffusion targets ($\log(0)=-\infty$). To ensure the log-probability vector remains well-defined throughout, we apply Laplace smoothing ($\alpha=1$) to the joint cell counts, adding one pseudo-count per cell prior to probability normalization. This operation provides a necessary mathematical floor of approximately $log(\frac{1}{N+K})\approx-9$ for sparse cells. Crucially, this perturbation is negligible in low-dimensional settings, altering cell probabilities by $<0.5\%$ at $K=32$.


\subsection{Distribution-level training target}
\label{sec:training_target}

%The central design decision is the choice of training unit. Rather than training on individual person records---which optimizes per-record likelihoods and provides only weak gradient signal for dependence structure---we treat each PUMA's empirical joint probability vector $\mathbf{p}$ as a single training example. Vectorization produces exactly 2,456 training examples, which directly align the learning objective with the evaluation metric: the model minimizes error on the quantity being measured.

To directly align the learning objective with our population-level evaluation metric, we define the training unit at the distribution level. Specifically, we treat each PUMA's empirical joint probability vector $\mathbf{p}$ as a single training example, yielding exactly 2,456 vectorized instances. This macroscopic approach provides a robust gradient signal for the dependence structure, overcoming the limitations of training on individual person records, which merely optimizes per-record likelihoods.

\paragraph{Log-probability representation.}
Each probability vector is mapped to log-probability space before training:
\[
  \mathbf{x}_0 = \log(\mathbf{p}+\epsilon), \quad \epsilon=10^{-6}.
\]

This logarithmic transformation serves a dual purpose. First, it compresses the dynamic range of near-zero cells that would otherwise disproportionately dominate the reconstruction error in absolute probability space. Second, it ensures the resulting distribution over $\mathbf{x}_0$ is approximately Gaussian, satisfying a fundamental prerequisite for the DDPM noise schedule to operate correctly.

%This representation compresses the dynamic range of near-zero cells, which would dominate reconstruction error in absolute probability space. And it produces a distribution over $\mathbf{x}_0$ that is approximately Gaussian (i.e., a prerequisite for the DDPM noise schedule to operate correctly).

\paragraph{Z-score normalization.}
%The log-probability vector is further standardized per training fold:
In its raw logarithmic form, the distribution of $\mathbf{x}_0$ is strongly shifted (e.g., mean $\approx-11$ and standard deviation $\approx4$ at $K=512$). This shift places the training targets far outside the standard normal $\mathcal{N}(0,I)$ regime assumed by the linear noise schedule. To correct this, the log-probability vector is strictly standardized per training fold:
\[
  \mathbf{z}_0 = \frac{\mathbf{x}_0 - \boldsymbol{\mu}_{\text{train}}}{\boldsymbol{\sigma}_{\text{train}}},
\]
where a small-variance floor of $10^{-6}$ is applied to $\boldsymbol{\sigma}_{\text{train}}$ to ensure numerical stability. Empirically, this z-score normalization is indispensable; omitting it consistently caused the generative process to collapse (yielding a near-random TVD of $\approx0.98$), as the unnormalized target distribution violates the fundamental assumptions of the linear noise schedule.


%with a small-variance floor of $10^{-6}$ applied to $\boldsymbol{\sigma}_{\text{train}}$. This step is critical: without it, the distribution of $\mathbf{x}_0$ is strongly shifted (mean $\approx-11$, std $\approx4$ at $K=512$), placing the training targets far outside the $\mathcal{N}(0,I)$ regime assumed by the linear noise schedule. Empirically, omitting z-score normalization collapses TVD to near-random ($\approx0.98$); with it, TVD recovers to competitive levels across all $K$ settings tested.

\paragraph{Denoiser architecture and training.}
The denoiser $\epsilon_\theta$ is parameterized as a two-hidden-layer multilayer perceptron (MLP) with hidden dimensions of [1024, 1024], sigmoid linear unit (SiLU) activations, and a 128-dimensional sinusoidal time embedding. To integrate the demographic constraints, the condition vector $\mathbf{c}$ is concatenated with both the noised input $\mathbf{z}_t$ and the time embedding at the initial layer. The model is trained to minimize the standard $\epsilon$-prediction mean squared error (MSE) loss:
%The denoiser $\epsilon_\theta$ is a two-hidden-layer MLP with hidden dimensions [1024, 1024], sigmoid linear unit (SiLU) activations, and a 128-dimensional sinusoidal time embedding. The condition vector $\mathbf{c}$ is concatenated with the noised input $\mathbf{z}_t$ and the time embedding at the first layer. Training minimizes the standard $\epsilon$-prediction mean squared error (MSE) loss:
\begin{equation}
  \mathcal{L} = \mathbb{E}_{t,\,\mathbf{z}_0,\,\boldsymbol{\epsilon}}\bigl[\|\boldsymbol{\epsilon} - \epsilon_\theta(\mathbf{z}_t, t, \mathbf{c})\|^2\bigr],
\end{equation}
%where $\mathbf{z}_t = \sqrt{\bar\alpha_t}\,\mathbf{z}_0 + \sqrt{1-\bar\alpha_t}\,\boldsymbol{\epsilon}$, $\boldsymbol{\epsilon}\sim\mathcal{N}(0,I)$, and $\bar\alpha_t$ follows a linear noise schedule ($\beta_1=10^{-4}$, $\beta_T=0.02$) with $T=1000$ timesteps. Optimizer: AdamW ($\text{lr}=10^{-3}$, $\text{wd}=10^{-4}$), gradient clipping at 1.0, batch size 512. Training epochs: 4,000 (2-var) and 30,000 (3/5-var $K\leq256$).
where $\mathbf{z}_t = \sqrt{\bar\alpha_t}\,\mathbf{z}_0 + \sqrt{1-\bar\alpha_t}\,\boldsymbol{\epsilon}$, $\boldsymbol{\epsilon}\sim\mathcal{N}(0,I)$, and $\bar\alpha_t$ follows a linear noise schedule ($\beta_1=10^{-4}$, $\beta_T=0.02$) spanning $T=1000$ timesteps. Optimization is performed using AdamW (learning rate $= 10^{-3}$, weight decay $= 10^{-4}$) with gradient clipping set to 1.0 and a batch size of 512. The training extends to 4,000 epochs for the 2-variable configuration and 30,000 epochs for the 3/5-variable configurations ($K\leq256$).

\subsection{Condition design}
\label{sec:condition_design}

To bridge the gap between the generative model and real-world data availability, we design conditioning signals ($\mathbf{c}$) that guide the diffusion process. These signals precisely encode the demographic constraints available to a practitioner at test time, representing aggregate statistics that can be derived from published census tables. 

These conditions are injected by concatenating $\mathbf{c}$ with the denoiser input \cite{ho2022cfg}. To systematically isolate the information contribution of different constraint types, we evaluate four distinct conditioning modes. For clarity, the input dimensions ($d_c$) are illustrated below using the 5-variable coarse setting (bin structure $B=(2,2,2,2,2)$, $K=32$):

%Conditions encode the demographic constraints available at test time---statistics that a practitioner can derive from published aggregate data without access to microdata. 

%Conditions are injected by concatenating $\mathbf{c}$ with the denoiser input \cite{ho2022cfg}. Four modes are tested; dimensions are given for the 5-var coarse setting with bin structure $B=(2,2,2,2,2)$, $K=32$:

\begin{itemize}
  \item \textbf{none} ($d_c=0$): Unconditional generation, which tests the model's learned prior over plausible copulas.
  \item \textbf{marginal} ($d_c=\textstyle\sum_d B_d=10$): Concatenated one-dimensional marginals that encode each attribute's frequency independently (readily available from standard census marginal tables).
  \item \textbf{pairwise} ($d_c=\textstyle\sum_{i<j}B_iB_j=40$): Concatenated two-dimensional cross-tabulations for all $\binom{D}{2}$ variable pairs. These directly encode second-order associations, providing the explicit structural information that a global-seed IPF inherently lacks.
  \item \textbf{marg+pair} ($d_c=50$): Both marginal and pairwise signals concatenated.
\end{itemize}

%These four modes form a controlled ablation ladder: each step adds a specific type of constraint, and the TVD reduction between consecutive steps quantifies the information value of that constraint type for copula recovery.
Together, these four modes form a controlled ablation ladder. Each step introduces a specific layer of constraint, ensuring that the TVD reduction between consecutive steps rigorously quantifies the information value of that constraint type for copula recovery.

\subsection{Sampling and post-processing}

To instantiate the synthetic population for an unseen target region, the generation pipeline translates the learned conditions into a discrete probability distribution. Specifically, the process proceeds as follows: (1)~draw initial noise $\mathbf{z}_T\sim\mathcal{N}(0,I)$; (2)~run the DDPM reverse denoising process conditioned on the region's specific vector $\mathbf{c}$ to yield $\hat{\mathbf{z}}_0$; (3)~apply the inverse z-score transformation to recover $\hat{\mathbf{x}}_0$; (4)~apply the softmax function to generate the raw probability estimate $\hat{\mathbf{p}}_{\text{raw}}$; and (5)~average 128 independent denoising draws to mitigate sampling noise and produce a robust final estimate.

While the diffusion model effectively captures the regional dependence structure, standard population synthesis requires absolute fidelity to known macro-level marginals. To enforce this agreement, we apply a single round of post-hoc IPF to the raw estimates.Crucially, this identical calibration is applied to the global-seed IPF baseline.By ensuring both pipelines are adjusted to match target marginals, this experimental design strictly isolates the seed quality (the accuracy of the underlying dependence struture) as the sole driver of any performance difference.


Given the region's census-reported population count $N$, the calibrated probability vector $\hat{\mathbf{p}}$ assigns $n_k=\mathrm{Round}(N\times\hat{p}_k)$ individuals to each demographic cell $k$.This procedure guarantees a final  synthetic population that faithfully reproduces both the target marginal frequencies and the localized dependence structure.

%For each test region, generation proceeds as follows: (1)~draw $\mathbf{z}_T\sim\mathcal{N}(0,I)$; (2)~run DDPM reverse denoising with the test region's condition vector $\mathbf{c}$, yielding $\hat{\mathbf{z}}_0$; (3)~apply the inverse z-score transform to obtain $\hat{\mathbf{x}}_0$; (4)~apply softmax to produce the raw probability estimate $\hat{\mathbf{p}}_{\text{raw}}$; (5)~average 128 independent draws to reduce Monte Carlo variance. 


\subsection{Evaluation protocol}
\label{sec:evaluation}

regional specific Comparing to the IPF method mentioned above, this framework could overcome the uniassumption of spatial homogeneity . 

To ensure the re
one round of post-hoc IPF then enforces exact marginal consistency. The same post-processing is applied to the global-seed IPF baseline, so that any performance difference reflects seed quality alone. Evaluation uses a leave-Michigan-out spatial split to prevent autocorrelation leakage (\S\ref{sec:evaluation}).

%from low-dimensional aggregated-level census data, overcoming the assumption of spatial homogeneity inherent in standard population synthesis methods. 

Recovering $\mathbf{p}$ from marginals alone is fundamentally underdetermined: $K-1$ unknowns versus $\sum_d(B_d-1)$ constraints. Traditional IPF resolves this by computing the information projection (I-projection) of a seed $\mathbf{q}$ onto the marginal-consistent polytope \cite{csiszar1975}, preserving the seed's interaction structure. However, when $\mathbf{q}$ is the population-weighted national average, every region inherits the same copula. Our diffusion model replaces this fixed $\mathbf{q}$ with a learned, region-specific estimate through three conceptual stages (Fig.~\ref{fig:framework}):

To rigorously evaluate the model's capacity to generalize across diverse geographic contexts, we adopt a strictly spatial leave-out evaluation framework. Unlike conventional random sampling, which often inflates apparent performance due to spatial autocorrelation leakage (e.g., neighboring regions sharing labor markets or commuting zones appearing in both the training and test sets), holding out an entire state ensures structural independence. Specifically, Michigan's 68 PUMAs form the isolated test set, while the remaining 2,388 PUMAs serve as the training set. For the 2-variable household configuration, we further partition the held-out Michigan regions into five folds of 13--14 PUMAs. This nested structure enables robust fold-level statistical validation using a one-sided sign test \cite{demsar2006}: under the null hypothesis that the diffusion model and the traditional IPF are equally likely to outperform one another in any given fold, the probability of the diffusion model winning all five folds strictly by chance is constrained to $(1/2)^5 = 0.031$.

To quantify the fidelity of the generated synthetic populations, our primary evaluation metric is the Total Variation Distance (TVD)[cite: 113], defined mathematically as:
$$
  \text{TVD} = \frac{1}{2}\sum_k|\hat{p}_k-p_k| ,
$$
where $\hat{\mathbf{p}}$ represents the generated joint distribution and $\mathbf{p}$ is the observed ground-truth distribution for a given PUMA. TVD ranges from 0 (indicating exact recovery) to 1 (indicating zero overlap). We contextualize model performance against two established reference baselines. The absolute upper bound of error is defined by the \textbf{Independence} baseline, which assumes zero interaction among attributes (computed as the outer product of target marginals) and yields a TVD of $\approx0.290$ at $K=32$. The primary competitive benchmark is the current standard practice, \textbf{IPF (global seed)}, wherein the population-weighted average joint distribution across all training PUMAs acts as the seed, subsequently adjusted to local target marginals via post-hoc IPF. Finally, cosine similarity is reported as an auxiliary metric to provide a complementary geometric perspective on distributional alignment.

%% ====================================================================
\section{Results}
\subsection{Generated joint distributions capture region-specific dependence}

To demonstrate that the diffusion model successfully learns localized dependence structures rather than defaulting to a smoothed national average, we first examine the generated synthetic populations at the microscopic level. We instantiate the joint probability vector $\hat{\mathbf{p}}\in\Delta^{K-1}$ into discrete person counts across $K=32$ demographic cells. As a test, we select two contrasting Michigan PUMAs: Oakland County (PUMA 2602903), representing a highly atypical region near the 90th percentile of spatial heterogeneity (TVD-to-global $= 0.170$), and Ionia et al. (PUMA 2601100), representing a typical region near the median (TVD-to-global $= 0.101$). 

These two case-study regions exhibit fundamentally distinct demographic compositions, with a between-region TVD of 0.253. This divergence is driven primarily by stark differences in socioeconomic status rather than basic demographics (age and sex differ only weakly, at 0.015 and 0.023, respectively). Specifically, the Oakland region exhibits a distinct "high-education" signature (63.4\% high education), with population mass heavily concentrated in the ``high-income, high-education'' and ``low-income, low-education'' joint cells (34.9\% and 34.0\%, respectively). Conversely, the median Ionia region is vastly more homogenous, dominated overwhelmingly by the ``low-income, low-education'' demographic (54.1\%), with substantially less mass in the highest socioeconomic tier (12.8\%). 

By evaluating the model's reconstruction accuracy across three hierarchical levels of complexity (Fig.~\ref{fig:output_examples}), we confirm that the diffusion pipeline captures these sharply contrasting local realities. First, at the one-dimensional macroscopic level, the generated marginal profiles for age, sex, income, education, and employment match the observed census data exactly, as guaranteed by the post-hoc IPF calibration (Fig.~\ref{fig:output_examples}a,d). 

Second, we assess the crucial two-dimensional education$\times$income cross-tabulation, which encodes the core localized dependence. Despite the structural contrast between the two PUMAs, the generated bivariate distributions align with the ground truth with near-perfect fidelity, achieving a cross-tab TVD of $1.9\times10^{-4}$ for the atypical Oakland region and $7.5\times10^{-4}$ for the median Ionia region (Fig.~\ref{fig:output_examples}b,e). 

Finally, expanding to the full multivariate structure, we evaluate the complete 32-cell joint distribution (Fig.~\ref{fig:output_examples}c,f). While discrepancies naturally increase because every higher-order interaction across the five attributes must be recovered simultaneously, the resulting full-joint TVD values remain exceptionally low (0.0209 for Oakland and 0.0153 for Ionia). These residual errors are roughly two orders of magnitude larger than the pairwise cross-tab errors, confirming that the dominant bivariate associations are perfectly resolved and the remaining variance lies in the ultra-fine multivariate tails. Crucially, the model reconstructs the highly atypical region just as accurately as the typical one, yielding results fully consistent with the robust 68-region test-set mean TVD of 0.024 (Table~\ref{tab:cond32}).

\begin{figure}[!bhtp]
\centering
\includegraphics[width=\textwidth]{figures/fig_03_output_examples.pdf}
\caption{Synthetic population outputs for two Michigan PUMAs chosen to represent high and median heterogeneity relative to the national-average copula. The top row shows PUMA~2602903, corresponding to Oakland County (East Central)--Troy \& Rochester Area PUMA; the bottom row shows PUMA~2601100, corresponding to Ionia, Montcalm, Mecosta \& Osceola Counties PUMA. Blue denotes observed values and orange denotes generated values. Panels~(a,d) report one-dimensional demographic profiles for age, sex, income, education, and employment. Panels~(b,e) report the education$\times$income cross-tabulation as grouped bars for the four cells, where L/L denotes low income and low education, L/H denotes low income and high education, H/L denotes high income and low education, and H/H denotes high income and high education. Panels~(c,f) report the complete 32-cell joint distribution in person counts, with cells ordered by true mass. Together, the three columns summarize agreement at the marginal, pairwise, and full-joint levels.}
\label{fig:output_examples}
\end{figure}

\FloatBarrier

\subsection{Verification and validation}
\label{sec:verification}

To verify that the diffusion model inherently internalizes the structural mapping from low-dimensional constraints to the full joint distribution, we first examine the raw generated outputs prior to any post-hoc IPF calibration (Fig.~\ref{fig:verification}a). Under pairwise conditioning at $K=32$, the raw marginal error is exceptionally low, with the TVD remaining consistently below 0.002 across all five attributes (e.g., 0.0009 for sex and 0.0015 for employment). This intrinsic marginal consistency demonstrates that the model produces nearly valid distributions by construction. It relies on the subsequent IPF step merely for exact mathematical alignment, rather than using it to repair gross generative errors.

We further validate the model's operational robustness by analyzing its training convergence and sampling stability. Both analyses are evaluated using the final post-IPF joint TVD to maintain consistency with our main benchmarking setup. Training convergence is remarkably rapid (Fig.~\ref{fig:verification}b). The mean joint TVD captures approximately 90\% of its total error reduction within the first 3,000 epochs (falling from 0.044 to 0.0256), before reaching a global minimum of 0.0236 at epoch 10,000. Extending training to 30,000 epochs yields a negligible shift (0.0238), indicating that the marginal-to-joint mapping is efficiently learnable and highly resilient to the choice of training horizon. 

Similarly, stochastic sampling stability exhibits a sharp early improvement followed by a definitive plateau (Fig.~\ref{fig:verification}c). Averaging just 16 denoising draws reduces the mean TVD from 0.0266 (single draw) to 0.0239, with performance fully stabilizing beyond 64 draws. At 128 draws, the run-to-run standard deviation across 10 random seeds diminishes to a mere $2.6\times10^{-5}$. Consequently, these diagnostics confirm that our reported performance gains are driven strictly by learned structural dependencies, entirely immune to stochastic sampling noise.

Finally, we test the model's geographic generalizability through a 5-fold cross-validation across Michigan's 68 held-out PUMAs, using the 5-variable $K=32$ person-level configuration. The spatial fold design prevents autocorrelation leakage by ensuring that no fold shares neighboring regions with the training set. Across five independent folds, the diffusion model outperforms IPF in every fold (one-sided sign test $p=0.031$), with a mean TVD difference of $-0.006 \pm 0.002$ (Table~\ref{tab:mi5fold}). This consistency confirms that the model's advantage is not an artifact of a single favorable spatial split, but reflects a genuine capacity to generalize region-specific dependence patterns.

\begin{figure}[!bhtp]
\centering
\includegraphics[width=\textwidth]{figures/fig_04_verification.pdf}
\caption{\textbf{Verification of model robustness, convergence, and sampling stability.} \textbf{a,} Raw marginal TVD by attribute under pairwise conditioning ($K=32$) prior to post-hoc IPF calibration. The extremely low error confirms the model's intrinsic capacity to satisfy macro-level constraints. \textbf{b,} Post-IPF joint TVD as a function of training epochs, demonstrating rapid convergence and insensitivity to overtraining. \textbf{c,} Post-IPF joint TVD as a function of the number of denoising draws. The minimal run-to-run standard deviation (across 10 random seeds) illustrates that stochastic sampling noise is effectively eliminated beyond 64 draws.}
\label{fig:verification}
\end{figure}
\FloatBarrier

\subsection{Constraint informativeness drives dependence recovery}

While the preceding results establish the model's capacity to generate accurate and generalizable joint distributions, they do not isolate the specific drivers of this dependence recovery. To systematically quantify the contribution of each information source, we perform a controlled ablation study. The model is trained under four distinct conditioning modes---no constraints, one-dimensional marginals only, two-dimensional pairwise cross-tabulations only, and a combination of both---and the resulting TVD is measured against the true joint distribution.

For the 5-variable coarse setting ($K=32$), the hierarchy of constraint informativeness is strikingly clear (Table~\ref{tab:cond32}; Fig.~\ref{fig:ablation}a). Even in the absence of any local constraints ($d_c=0$), the diffusion model learns a structured prior over plausible copulas, achieving a TVD of 0.114. While this substantially outperforms statistical independence (0.290), it remains distant from the true local dependence. Injecting one-dimensional marginals ($d_c=10$) supplies essential local frequency information, reducing the TVD to 0.043. Crucially, upgrading to pairwise cross-tabulations ($d_c=40$) further drives the TVD down to 0.024---a massive 44\% relative reduction compared to the marginal-only baseline. Interestingly, concatenating marginals on top of pairwise constraints yields negligible marginal utility (0.0239 versus 0.0240), indicating that once second-order structural associations are provided, the remaining first-order frequency information becomes structurally redundant.

This hierarchical pattern reflects a fundamental information-theoretic bottleneck rather than a mere architectural limitation. At $K=32$, the target joint distribution contains 31 free parameters, yet one-dimensional marginals provide only 10 constraint values. Pairwise cross-tabulations inject 40 additional values that directly encode second-order associations, effectively closing the informational gap. 

Consequently, this logic scales predictably across finer resolutions and varying dimensionalities (Fig.~\ref{fig:ablation}b,c). In the 5-variable $K=128$ setting, replacing marginals with pairwise conditioning reduces the TVD from 0.070 to 0.051 (a 26\% reduction). In the 3-variable fine setting ($K=256$), augmenting marginals with pairwise information yields a 17\% reduction (from 0.101 to 0.084). As summarized in Figure~\ref{fig:ablation}c, the incremental value of pairwise information scales with the constraint deficit: it is logically negligible at $D=2$ (where the pairwise table is the joint itself), but rises dramatically to 17\% at $D=3$ and peaks at 26--44\% at $D=5$. Dependence recovery, therefore, is driven not merely by expanding model capacity, but by supplying higher-order constraints that bridge the gap between aggregated macroscopic observations and the full microscopic joint.

\begin{table}[!bhtp]
\centering
\caption{Condition ablation for the 5-variable coarse setting ($K=32$).}
\label{tab:cond32}
\begin{tabular}{lcc}
\toprule
Condition & cond\_dim & TVD \\
\midrule
Independence baseline & ---  & 0.290 \\
none      & 0  & 0.114 \\
marginal  & 10 & 0.043 \\
pairwise  & 40 & 0.024 \\
marg+pair & 50 & 0.024 \\
\bottomrule
\end{tabular}
\vspace{2pt}
\begin{minipage}{0.98\linewidth}
\footnotesize
\textit{Notes:} ``Independence baseline'' assumes all attributes are statistically independent. ``none'' uses no condition, ``marginal'' uses one-dimensional marginals, ``pairwise'' uses two-dimensional cross-tabulations, and ``marg+pair'' uses both.
\end{minipage}
\end{table}

\begin{figure}[!bhtp]
\centering
\includegraphics[width=\textwidth]{figures/fig_05_ablation.pdf}
\caption{Condition ablation and information hierarchy.
(a)~TVD across conditioning modes at $K=32$, with the independence baseline as reference.
(b)~TVD across conditioning modes at $K=128$.
(c)~Pairwise contribution, measured as the relative TVD reduction from marginal-only to the best pairwise-augmented condition, across variable counts.}
\label{fig:ablation}
\end{figure}

\FloatBarrier
\subsection{Comparison with iterative proportional fitting}

Ultimately, the practical utility of this generative framework hinges on its performance relative to the current industry standard: iterative proportional fitting (IPF) initialized with a global (national-average) seed. To ensure a strictly fair comparison, the final step in both pipelines is the identical post-hoc IPF projection; the singular difference lies in the quality of the seed supplied to that projection. Traditional IPF relies on the training-set weighted-average joint as a static global seed, whereas our diffusion model synthesizes a dynamic, region-specific seed conditioned on available local statistics. This design strictly isolates seed quality---the accuracy of the underlying dependence structure---as the sole benchmark metric.

Across all four person-level configurations, the distribution-level diffusion model consistently generates higher-quality seeds than the global average, yielding relative TVD reductions ranging from 19\% to 59\% (Table~\ref{tab:main}; Fig.~\ref{fig:comparison}a,d). The most pronounced advantage emerges in the 5-variable coarse setting ($K=32$), where pairwise-conditioned diffusion achieves a TVD of 0.024, eclipsing the IPF baseline of 0.058. This advantage persists across higher resolutions: it remains robust at $K=128$ (0.051 vs.\ 0.081) and $K=256$ (0.084 vs.\ 0.103), as well as in the 3-variable setting ($K=64$; 0.055 vs.\ 0.070).

The Michigan 5-fold cross-validation (\S\ref{sec:verification}) further confirms that this advantage generalizes to unseen regions: diffusion outperforms IPF in every fold ($p = 0.031$; Fig.~\ref{fig:comparison}b), ruling out test-set artifacts.

\begin{table}[!bhtp]
\centering
\caption{Comparison between diffusion and IPF across configurations ($K\leq256$).}
\label{tab:main}
\begin{tabular}{lccccc}
\toprule
Configuration & $K$ & Best condition & Diff.\ TVD & IPF TVD & Rel.\ $\Delta$ \\
\midrule
5-var coarse  & 32  & pairwise       & 0.024 & 0.058 & $-59\%$ \\
5-var medium  & 128 & pairwise       & 0.051 & 0.081 & $-37\%$ \\
3-var fine    & 256 & marg+pair      & 0.084 & 0.103 & $-19\%$ \\
3-var         & 64  & marginal       & 0.055 & 0.070 & $-22\%$ \\
\bottomrule
\end{tabular}
\vspace{2pt}
\begin{minipage}{0.98\linewidth}
\footnotesize
\textit{Notes:} All configurations use person-level attributes (AGEP, SEX, PINCP, SCHL, ESR). ``5-var coarse'' and ``5-var medium'' are two binning resolutions of the same five-variable setting ($K=32$ and $K=128$), and ``3-var fine'' is the fine three-variable setting ($K=256$). ``marg+pair'' means conditioning on both one-dimensional marginals and two-dimensional pairwise cross-tabulations.
\end{minipage}
\end{table}


\begin{table}[!bhtp]
\centering
\caption{Michigan 5-fold statistical test: diffusion vs.\ IPF ($p=0.031$; 5-var, person-level, $K=32$).}
% TODO: Replace with person-level 5-fold results once partner completes the experiment.
\label{tab:mi5fold}
\begin{tabular}{lccc}
\toprule
Fold & Diffusion TVD & IPF TVD & $\Delta$ \\
\midrule
0 & 0.077 & 0.080 & $-0.003$ \\
1 & 0.069 & 0.075 & $-0.005$ \\
2 & 0.066 & 0.073 & $-0.007$ \\
3 & 0.066 & 0.074 & $-0.007$ \\
4 & 0.065 & 0.071 & $-0.006$ \\
\midrule
Mean & 0.069 & 0.074 & $-0.006$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!bhtp]
\centering
\includegraphics[width=\textwidth]{figures/fig_06_main_results.pdf}
\caption{Comparison with IPF.
(a)~Diffusion TVD versus IPF TVD across all configurations. Points below the diagonal indicate diffusion advantage.
(b)~Per-fold $\Delta$ (diffusion $-$ IPF) in the Michigan 5-fold household-level evaluation.
(c)~Relative gain (\%) across the five reported configurations.
(d)~Paired TVD comparison for each configuration.}
\label{fig:comparison}
\end{figure}


\FloatBarrier


\section{Discussion}

The condition ablation reveals a structural insight about synthesis as an inverse problem: the critical resource is not model capacity but constraint informativeness. From none to marginal to pairwise, each additional layer of constraint information reduces error by a large, discrete step. Model architecture matters only at the margin. The advantage reverses at $K=512$, where two mechanisms jointly prevent recovery: cell sparsity ($\sim$14 persons per cell vs.\ $\sim$220 at $K=32$) and constraint insufficiency (128 pairwise parameters against 511 joint unknowns), so that even extending training from 10k to 30k epochs does not close the gap (Supplementary Table~S1, Fig.~S1). This reframes the scaling boundary not as a model failure but as an information-theoretic limit: enriching input constraints is more effective than increasing model capacity or training budget.

The choice of training target---predicting regional distributions rather than individual records---has implications beyond this specific model. Tabular generative models trained on individual samples \cite{kotelnikov2023,stoian2024} optimize per-record quality, which does not guarantee population-level distributional accuracy---a tension between feasibility and diversity that recent work has begun to formalize \cite{kim2022}. For synthesis tasks evaluated on regional joint statistics---the standard in transportation and urban modeling---training directly on the aggregate quantity of interest aligns the learning objective with the evaluation metric, and our results suggest this alignment is decisive.

The dependence heterogeneity finding (mean TVD 0.19 across 2,456 regions) has independent value and provides a quantitative frame for interpreting model performance. The 0.19 TVD represents the average deviation of regional dependence from the global seed that IPF uses. At $K=32$, pairwise-conditioned diffusion reduces TVD from 0.058 (IPF) to 0.024---closing approximately 60\% of this gap. At $K=128$, it closes about 37\%. These numbers give a concrete answer to ``what did the model learn'': it recovered a substantial fraction of the spatial dependence variation that IPF's global seed ignores. This motivates not only learned approaches but also stratified IPF schemes that use cluster-specific seeds.

Limitations are explicit. First, experiments use United States PUMS; generalization to other countries depends on comparable marginal systems. Second, pairwise conditions require cross-tabulations that may not always be publicly available, though they can be estimated from microdata samples. Third, the 2-var and 3/5-var settings use different statistical units (household vs.\ person); cross-setting comparisons serve as scaling diagnostics, not same-unit absolute rankings. Fourth, the current model does not enforce marginal constraints architecturally---this is handled by post-hoc IPF, which adds a processing step.

A concrete path forward: enrich constraints before enlarging models. Higher-order cross-tabulations are ideal but often unavailable. Tract-level auxiliary descriptors---built environment, labor structure, university/industrial profiles---offer a feasible route to increase conditional identifiability in high-dimensional settings. More broadly, population synthesis can be viewed as an instance of ecological inference \cite{king1997}---reconstructing individual-level structure from aggregate observations---for which distribution-level generative models provide a natural probabilistic language. Extending the present framework to sub-regional disaggregation, where building-level spatial priors replace marginal constraints as the conditioning signal, is an immediate next step enabled by increasingly available building footprint and land-use data \cite{rineer2025}.

%% ====================================================================
\FloatBarrier
\section{Data availability}
The underlying data are ACS PUMS public-use files, available from the United States Census Bureau. Processed joint probability tables and experiment outputs are archived in the project repository.

\section{Code availability}
All scripts for data processing, model training, sampling, and evaluation are available in the project repository.

\bibliographystyle{unsrt}
\bibliography{references}

%% ====================================================================
\clearpage
\appendix
\section*{Supplementary Information}

\subsection*{S1. Dimensional scaling and the information-sparsity boundary}

All five configurations with $K\leq256$ show diffusion advantage over IPF. Testing whether this holds at higher dimensionality, we train on $K=512$ (fine-grained 5-variable joint: $4\times2\times4\times4\times4$ bins). Diffusion TVD reaches 0.139 while IPF achieves 0.112, a reversal of $+0.028$ (Supplementary Table~S1).

Two mechanisms drive this reversal. First, \textit{cell sparsity}: at $K=32$, each joint cell contains $\sim$220 persons on average; at $K=512$, only $\sim$14, making empirical probability estimates unreliable as training targets. Second, \textit{constraint insufficiency}: the pairwise condition vector provides 128 parameters, but the $K=512$ joint has 511 free parameters---a coverage ratio of 25\%, versus 129\% at $K=32$. Extending training from 10k to 30k epochs reduces TVD from 0.182 to 0.147 ($-$19\%) but does not reach IPF's 0.112, confirming the bottleneck is information, not optimization. A counterintuitive corollary: pairwise conditioning at $K=512$ (TVD = 0.249) is \textit{worse} than marginal-only (0.147), because the 128-dim pairwise input must reconstruct 511 unknowns without the global-prior support that IPF supplies to the marginal-only pipeline.

\begin{table}[htbp]
\centering
\caption{Supplementary Table S1. Dimensional scaling comparison between diffusion and IPF.}
\label{tab:S1_Kscale}
\begin{tabular}{ccccc}
\toprule
$K$ & Variables & Diff.\ TVD & IPF TVD & $\Delta$ \\
\midrule
32  & 5-var & 0.024 & 0.058 & $-0.034$ \\
64  & 2-var & 0.069 & 0.074 & $-0.006$ \\
64  & 3-var & 0.055 & 0.070 & $-0.015$ \\
128 & 5-var & 0.051 & 0.081 & $-0.030$ \\
256 & 3-var & 0.084 & 0.103 & $-0.019$ \\
512 & 5-var & 0.139 & 0.112 & $+0.028$ \\
\bottomrule
\end{tabular}
\vspace{2pt}
\begin{minipage}{0.98\linewidth}
\footnotesize
\textit{Notes:} $\Delta = \text{Diffusion TVD} - \text{IPF TVD}$ (negative is better for diffusion). For each $K$, the diffusion entry reports the best available condition setting under the corresponding experiment protocol.
\end{minipage}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/fig_s01_scaling.pdf}
\caption{\textbf{Supplementary Fig.~S1.} Dimensional scaling and information boundaries.
(a)~Diffusion TVD and IPF TVD vs.\ joint dimensionality $K$. Crossover near $K\approx 400$.
(b)~Training effect at $K=512$: TVD at 10k and 30k epochs, with IPF reference.
(c)~Average persons per cell vs.\ $K$, illustrating sparsity growth.
(d)~Constraint coverage: pairwise condition dimensions vs.\ joint free parameters.}
\label{fig:S1_scaling}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/fig_s02_heterogeneity_stats.pdf}
\caption{\textbf{Supplementary Fig.~S2.} Distribution of dependence heterogeneity.
(a)~Histogram of TVD-to-global for 2,456 regions in the United States. Mean = 0.191 (dashed); 90th percentile = 0.265.
(b)~Rank-ordered TVD across all regions; the long right tail shows a substantial minority deviate strongly from the national average.
(c)~Same metric for Michigan's 68 regions (the held-out test state), showing that Michigan spans a representative range of heterogeneity levels.}
\label{fig:S2_heterogeneity}
\end{figure}

\end{document}
